# 🔧 إصلاح مشكلة البناء على Netlify

## ❌ المشكلة

```
sh: 1: vite: not found
Build script returned non-zero exit code: 2
```

**السبب:** التبعيات (dependencies) لم يتم تثبيتها قبل البناء.

---

## ✅ الحل

تم تحديث `netlify.toml` ليقوم بتثبيت التبعيات قبل البناء:

### قبل (❌):
```toml
command = "npm run build"
```

### بعد (✅):
```toml
command = "npm ci && npm run build"
```

---

## 📝 التغييرات

### 1. تحديث netlify.toml
- ✅ إضافة `npm ci` قبل `npm run build`
- ✅ إضافة `NPM_FLAGS = "--legacy-peer-deps"` للتعامل مع peer dependencies

### 2. ما هو `npm ci`؟
- أسرع من `npm install`
- مثالي لبيئة CI/CD
- يستخدم `package-lock.json` بالضبط
- يحذف `node_modules` ويبني من الصفر

---

## 🚀 الخطوات التالية

### 1. Commit التغييرات
```bash
git add netlify.toml
git commit -m "fix: إصلاح مشكلة البناء على Netlify"
git push origin main
```

### 2. Netlify سيعيد البناء تلقائياً
- بعد push، Netlify سيكتشف التغييرات
- سيعيد البناء تلقائياً
- ✅ يجب أن ينجح الآن!

### 3. أو إعادة البناء يدوياً
في Netlify Dashboard:
- اذهب إلى **Deploys**
- اضغط **"Trigger deploy"** → **"Deploy site"**

---

## 🔍 التحقق

بعد إعادة البناء، يجب أن ترى:
```
✓ Installing dependencies
✓ Building site
✓ Deploying site
✅ Site is live!
```

---

## 📚 معلومات إضافية

### لماذا `npm ci` وليس `npm install`؟
- ✅ أسرع (لا يحدث package.json)
- ✅ أكثر موثوقية (يستخدم lock file بالضبط)
- ✅ مناسب لبيئة الإنتاج
- ✅ يحذف node_modules أولاً (نظيف)

### متى نستخدم `npm install`؟
- في التطوير المحلي
- عند إضافة حزمة جديدة
- عند تحديث التبعيات

---

## 🆘 إذا استمرت المشكلة

### 1. تحقق من package-lock.json
```bash
# تأكد من وجوده
ls package-lock.json

# إذا لم يكن موجوداً
npm install
git add package-lock.json
git commit -m "chore: add package-lock.json"
git push
```

### 2. تحقق من Node Version
في `netlify.toml`:
```toml
NODE_VERSION = "18"
```
يجب أن يكون متوافقاً مع مشروعك.

### 3. تحقق من Build Logs
في Netlify Dashboard:
- اذهب إلى **Deploys** → **Build log**
- ابحث عن أخطاء محددة

### 4. جرب Build محلياً
```bash
# تأكد من أن البناء يعمل محلياً
npm ci
npm run build

# إذا نجح محلياً، المشكلة في Netlify config
```

---

## ✅ الخلاصة

**المشكلة:** vite: not found  
**السبب:** التبعيات لم تُثبت  
**الحل:** إضافة `npm ci` قبل البناء  
**النتيجة:** ✅ يجب أن يعمل الآن!

---

**🎉 بعد push التغييرات، Netlify سيعيد البناء تلقائياً ويجب أن ينجح!**

