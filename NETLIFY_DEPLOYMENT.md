# دليل نشر محفظة الآخرة على Netlify

## الطريقة الأولى: النشر عبر واجهة Netlify (الأسهل)

### الخطوات:

#### 1. تحضير المشروع
```bash
# تأكد من أن كل شيء يعمل محلياً
npm install
npm run build
```

#### 2. إنشاء حساب على Netlify
- اذهب إلى [Netlify](https://netlify.com)
- سجل دخول بحساب GitHub الخاص بك

#### 3. ربط المشروع
1. اضغط على **"Add new site"** → **"Import an existing project"**
2. اختر **GitHub** وابحث عن repository الخاص بك
3. املأ الإعدادات:
   ```
   Branch to deploy: main
   Build command: npm run build
   Publish directory: dist
   ```

#### 4. إضافة متغيرات البيئة (Environment Variables)
في إعدادات الموقع:
1. اذهب إلى **Site settings** → **Environment variables**
2. أضف المتغيرات التالية:
   ```
   VITE_SUPABASE_URL = [your_supabase_url]
   VITE_SUPABASE_ANON_KEY = [your_supabase_anon_key]
   ```

#### 5. انشر الموقع
- اضغط **"Deploy site"**
- انتظر حتى ينتهي البناء (حوالي 2-3 دقائق)
- ✅ موقعك الآن على الإنترنت!

---

## الطريقة الثانية: النشر عبر Netlify CLI

### الخطوات:

#### 1. تثبيت Netlify CLI
```bash
npm install -g netlify-cli
```

#### 2. تسجيل الدخول
```bash
netlify login
```

#### 3. تهيئة المشروع
```bash
netlify init
```

#### 4. إضافة متغيرات البيئة
```bash
netlify env:set VITE_SUPABASE_URL "your_supabase_url"
netlify env:set VITE_SUPABASE_ANON_KEY "your_supabase_anon_key"
```

#### 5. بناء ونشر
```bash
# بناء المشروع
npm run build

# نشر على Netlify
netlify deploy --prod
```

---

## الطريقة الثالثة: السحب والإفلات (Drag & Drop)

### الخطوات:

#### 1. بناء المشروع محلياً
```bash
npm install
npm run build
```

#### 2. اذهب إلى Netlify Drop
- افتح [Netlify Drop](https://app.netlify.com/drop)
- اسحب مجلد `dist` وأفلته في المتصفح
- ✅ سيتم نشر الموقع فوراً!

**⚠️ ملاحظة:** هذه الطريقة لا تدعم متغيرات البيئة، لذا يجب إضافتها يدوياً في `.env` قبل البناء.

---

## إعدادات متقدمة

### 1. Custom Domain (نطاق مخصص)
1. اذهب إلى **Domain settings**
2. اضغط **"Add custom domain"**
3. اتبع التعليمات لربط النطاق

### 2. HTTPS
- ✅ مفعّل تلقائياً على Netlify
- شهادة SSL مجانية من Let's Encrypt

### 3. النشر التلقائي
- ✅ مفعّل تلقائياً
- كل push على branch `main` = نشر جديد

### 4. معاينة Pull Requests
- ✅ مفعّل تلقائياً
- كل PR = موقع معاينة خاص

---

## الملفات المطلوبة للنشر

تم إنشاء الملفات التالية:
- ✅ `netlify.toml` - إعدادات Netlify
- ✅ `public/_redirects` - إعادة توجيه React Router
- ✅ `.env.example` - مثال على متغيرات البيئة

---

## استكشاف الأخطاء

### المشكلة: صفحة 404 عند التنقل
**الحل:** تأكد من وجود ملف `public/_redirects`

### المشكلة: خطأ في الاتصال بـ Supabase
**الحل:** 
1. تحقق من متغيرات البيئة في Netlify
2. تأكد من أن Supabase URL وKey صحيحة

### المشكلة: فشل البناء
**الحل:**
1. تحقق من الـ build logs
2. تأكد من أن `npm run build` يعمل محلياً
3. تحقق من إصدار Node.js (يجب أن يكون 18+)

### المشكلة: الموقع بطيء
**الحل:**
1. تفعيل Asset Optimization في إعدادات Netlify
2. استخدام CDN (مفعّل تلقائياً)
3. تحسين الصور

---

## الأداء والتحسينات

### ✅ مفعّل تلقائياً على Netlify:
- 🚀 CDN عالمي
- 📦 Compression (Gzip/Brotli)
- 🎯 Smart CDN
- 🔒 HTTPS
- ⚡ HTTP/2
- 📱 Instant Rollbacks
- 🌍 Edge Network

### يمكن تفعيلها:
- Asset Optimization
- Form Detection
- Analytics
- Split Testing
- Serverless Functions

---

## الموارد المفيدة

- 📚 [Netlify Documentation](https://docs.netlify.com/)
- 🎓 [React + Vite on Netlify](https://docs.netlify.com/integrations/frameworks/vite/)
- 💬 [Netlify Community](https://answers.netlify.com/)
- 🐛 [Netlify Status](https://www.netlifystatus.com/)

---

## الخلاصة

### الطريقة الموصى بها:
✅ **الطريقة الأولى** (GitHub + Netlify)
- أسهل
- نشر تلقائي
- معاينة PR
- يدعم متغيرات البيئة

### للنشر السريع:
✅ **الطريقة الثالثة** (Drag & Drop)
- فوري
- لا يحتاج GitHub
- مناسب للتجربة

---

**🎉 بارك الله فيك! موقعك الآن على الإنترنت ويساعد المسلمين في استثمار آخرتهم!**

